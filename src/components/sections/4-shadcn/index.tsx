import { classNames } from "@/utils";
import { HTMLAttributes } from "react";

export function Section4_Chadcn({ className }: HTMLAttributes<HTMLUListElement>) {
    return (
        <div className={classNames("h-full bg-background overflow-hidden flex flex-col", className)}>
            <div className="h-full border-muted border-b">
11
            </div>
        </div>
    );
}
